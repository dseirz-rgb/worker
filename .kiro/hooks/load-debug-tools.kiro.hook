{
  "enabled": true,
  "name": "🔧 加载前端调试工具",
  "description": "手动触发加载 web-debugging skill 并启用 chrome-devtools MCP，用于前端调试",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请执行以下操作来加载前端调试工具：\n\n1. 运行 `openskills read web-debugging` 加载 web-debugging skill\n2. 编辑 `.kiro/settings/mcp.json`，将 chrome-devtools MCP 的 `disabled` 改为 `false`\n3. 确认 MCP 已启用并可用\n\n完成后告知用户可用的调试工具列表。"
  }
}