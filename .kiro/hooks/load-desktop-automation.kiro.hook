{
  "enabled": true,
  "name": "🖥️ 加载桌面自动化",
  "description": "加载 desktop-automation skill 并启用 ui-tars MCP，用于桌面自动化、截图等功能",
  "version": "1",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "type": "askAgent",
    "prompt": "请执行以下步骤加载桌面自动化功能：\n\n1. 运行 `openskills read desktop-automation` 加载 skill 文档\n2. 编辑 `.kiro/settings/mcp.json`，找到 ui-tars MCP 配置，将 `disabled` 改为 `false`\n3. 确认 MCP 已启用，可以使用截图、窗口管理等桌面自动化工具"
  }
}